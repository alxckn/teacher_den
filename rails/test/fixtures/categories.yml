# == Schema Information
#
# Table name: categories
#
#  id               :integer          not null, primary key
#  label            :string
#  displayable_name :string
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#

colles:
  displayable_name: Colles
  label: colles

ds:
  displayable_name: Devoirs surveillés
  label: ds
